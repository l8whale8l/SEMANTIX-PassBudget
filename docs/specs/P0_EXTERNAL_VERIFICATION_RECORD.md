# P0 external verification record

- Review date: 2026-09-04 (original cross-tool evidence: 2026-09-03)
- Scope: cross-tool orbit calculation and the lean P0 orbit-to-capacity-to-priority flow
- PM verdict: **P0_CORE_ACCEPTED**, baseline `d467468`; no PR merge authorised by this record
- Required CI status: **PASS on d467468 (4/4 jobs)**; subsequent commits require their own green CI

## 1. What was verified

The verification answers one bounded question:

> Given the same orbit, ground-station coordinates, elevation mask, time window and numerical
> conventions, does the product implementation calculate the same AOS, LOS, pass duration and
> maximum elevation as an independently implemented orbit tool?

It does not certify KMU-ET02 performance, radio-link throughput, RF reception quality or real-world
pointing accuracy.

## 2. Implementations and versions

| Role | Implementation | Version | Distribution role |
|---|---|---|---|
| Product calculation | `python-sgp4` plus `semantix_passbudget.adapters.orbit` | `sgp4 2.27`, Python 3.12.13 | Product-side implementation |
| Independent oracle | NASA GMAT `SPICESGP4` and `ContactLocator` | GMAT R2026a | Offline evidence generation only |
| Database verification | PostgreSQL | 17.6 | Disposable local test cluster |

GMAT is not included in the product and is not needed at runtime. Orekit was not executed in this
verification. For the lean P0 scope, GMAT is the required independent oracle and Orekit is an
optional future confidence check, not a release blocker. Architecture documents must state this
consistently.

## 3. Frozen inputs and method

Two evidence sets were compared:

- `EVD-ORB-01`: SGP4/TLE propagation with synthetic ground-station coordinates.
- `EVD-ORB-02`: a synthetic 700 km circular, 98-degree inclination, `TWO_BODY_V1` orbit with
  synthetic ground-station coordinates.

Both sides used the same documented time, frame, Earth-model, elevation-mask and clipping rules.
The product output was compared with static expected tables parsed from preserved GMAT reports.
Expected values were not generated by the product implementation. The executable comparison is
`tests/integration/test_orbit_cross_tool.py`; the complete numerical contract is
`docs/specs/ORBIT_VERIFICATION_CONTRACT.md`.

Coverage includes ordinary multi-pass windows, analysis-window start/end clipping, zero-contact
stations, low-elevation passes and a tangent pass close to the 5-degree elevation mask.

## 4. Acceptance ceilings and observed results

| Quantity | Acceptance ceiling | EVD-ORB-01 worst | EVD-ORB-02 worst | Result |
|---|---:|---:|---:|---|
| Pass count | Exact match | 13 matched | 45 matched | PASS |
| AOS | 1.000 s | 0.017031 s | 0.013835 s | PASS |
| LOS | 1.000 s | 0.007365 s | 0.013816 s | PASS |
| Pass duration | 2.000 s | 0.024554 s | 0.025763 s | PASS |
| Maximum elevation | 0.050 deg | 0.005075 deg | 0.004992 deg | PASS |
| Maximum-elevation time | 2.000 s | 0.010703 s | 0.030667 s | PASS |
| Zero-contact stations | Exact match | Matched | Matched | PASS |
| Repeated-run difference | Zero | Zero | Zero | PASS |

Total: 58 passes compared, zero ceiling breaches. A PM-side rerun of the committed-style
cross-tool test on 2026-09-03 produced **22 passed in 11.16 seconds**.

## 5. Defect found by the independent comparison

The first `EVD-ORB-02` comparison failed with an AOS difference of up to 22.3 seconds and an
Earth-fixed position difference of 18.29 km. The inertial states agreed, isolating the problem to
the frame transformation. The IAU-76 precession middle rotation `R2(theta)` had been implemented
about the X axis instead of the Y axis. Correcting the axis reduced the Earth-fixed discrepancy to
within 0.1 m and brought the fixture inside every ceiling.

This is the strongest evidence that the cross-tool gate was useful: the faulty output was
plausible and would not have been caught by product self-comparison.

## 6. Historical supporting results (2026-09-03, before product integration)

| Check | Result |
|---|---|
| Full suite with disposable PostgreSQL | 376 passed, 0 skipped, 0 failed |
| Suite without PostgreSQL | 366 passed, 10 PostgreSQL tests skipped |
| PostgreSQL marker suite | 10 passed on PostgreSQL 17.6 |
| Ruff, formatting, mypy, secret scan | PASS |
| Alembic heads, offline SQL and live upgrade | PASS |
| Wheel clean install and existing golden hashes | PASS |

These are historical local results, not counts for the integrated product. The current baseline
is recorded separately below; skipped tests are never counted as passing.

### Integrated product baseline (2026-09-04, d467468)

- PM rerun: **378 passed, 11 skipped, 2 warnings**. All 11 local skips were PostgreSQL cases,
  not 10. Both orbit evidence sets were present locally.
- Orbit cross-tool plus API/CLI flow tests: **31 passed** locally.
- Public checkout omits `EVD-ORB-01`: its 11 cross-tool cases are additional skips, separate from
  the 11 PostgreSQL cases. SGP4 has local cross-tool evidence only; `TWO_BODY_V1` has public evidence.
- Required `backend` CI: four jobs succeeded, including wheel install, dependency audit, and
  **Docker image build, API execution and SQLite result retrieval after restart**.
- [PR CI run](https://github.com/l8whale8l/SEMANTIX-PassBudget/actions/runs/33773033361)
- [Push CI run](https://github.com/l8whale8l/SEMANTIX-PassBudget/actions/runs/33772586444)
- Optional PostgreSQL workflow was not run for this branch. H100 and external GMAT regeneration
  were not run in the integration closeout. Docker restart was run and must not be listed as unrun.

The `PB-GOLDEN-ORB-01` rerun produced 7 passes, 4,212.443830 s contact time,
526,555,476 B capacity, 522,000,000 B allocated, and 158,000,000 B backlog. Wildfire and aircraft
outputs completed; ship output was partial. These are assumed inputs, not mission measurements.
The reproduced hashes at `d467468` are:

- Input: `c2cb1681b11cf567c0d03ca0e6429d3186cd6d010e201914f73ae854097dd419`
- Result: `279118d332850cefd614b53019eadeef2a0f4e73bc26e30ef737aab4b4086697`

The earlier handoff's abbreviated input hash `1c44...` does not match this reproduced value and
is corrected here; this correction is not a change to the calculation or canonical format.

## 7. Explicit limitations

- Every station coordinate in the evidence is synthetic and is not a verified KMU ground station.
- `EVD-ORB-01` uses a non-KMU object; `EVD-ORB-02` is entirely synthetic.
- UT1-UTC and polar motion are forced to zero for implementation comparability. The result proves
  agreement on a defined problem, not absolute real-world timing.
- Atmospheric refraction, horizon masks, light time and operational station availability are not
  modelled.
- No RF link budget, modem performance, packet loss or real reception data was validated.
- The raw CelesTrak-derived TLE must not be published until redistribution rights are resolved; a
  licence-clear or project-generated synthetic TLE is preferred for the public fixture.
- The verification was executed locally with an external calculation tool. It is cross-tool
  verification, not an independent audit performed by a separate V&V organisation.

## 8. PM release decision

The orbit **evidence gate is accepted** for the lean P0 scope. Additional Orekit work, H100 testing,
high-fidelity Earth orientation, Docker restart testing and PostgreSQL operations are not required
to proceed with the core product.

`ORBIT_DERIVED` is now accepted by the domain and connected to API and CLI. The lean P0 status is:

`P0_CORE_ACCEPTED`

The core path has been completed and verified at `d467468`:

1. Orbit/TLE and station inputs generate `ORBIT_DERIVED` contact windows.
2. Generated contact duration feeds the existing assumed-throughput capacity calculation.
3. Capacity feeds the existing model-output prioritisation flow.
4. The end-to-end golden scenario includes explicit assumption/provenance labels.
5. Required GitHub Actions passed on Ubuntu.

The user can now estimate contact time, transfer capacity and model-output priorities under stated
assumptions. This does not certify real delivery or KMU-ET02 performance. Orekit, H100 and optional
database operations remain out of the lean P0 critical path. PR merge remains a user decision.

## 9. Evidence-loader closeout (2026-09-04)

Missing inputs may skip only for deliberately withheld `EVD-ORB-01`. Missing `EVD-ORB-02` inputs,
unknown fixture inputs, or a missing expected table for a fixture whose inputs are present must
fail. Five isolated regression cases enforce this without moving or deleting real evidence.
No oracle values, tolerances, product calculations or golden hashes are changed by this closeout.
Local closeout verification: **383 passed, 11 PostgreSQL skipped, 2 deprecation warnings**;
orbit cross-tool tests **27 passed**; Ruff lint/format, mypy, secret scan and all golden checks PASS.
The additional five tests are guard regressions, not five new orbit observations.
Its new commit must pass CI independently; the `d467468` links above certify the baseline only.
